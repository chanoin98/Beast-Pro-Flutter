name: xiao_ble
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_blue: ^0.8.0
  flutter_timer_countdown: ^1.0.5
  battery_indicator: ^0.1.0
  sizer: ^2.0.15
  google_fonts: ^3.0.1
  flutter_spinkit: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  flutter_launcher_icons: ^0.11.0

flutter_icons:
  android: true
  ios: true
  image_path: "assets/app_ic.png"

flutter:

  uses-material-design: true

  assets:
    - assets/
